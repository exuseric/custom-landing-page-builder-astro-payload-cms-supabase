---
import type { HeroBlock } from "@utils/payload/types";
import Container from "@components/Container.astro";
import s from "./style.module.css";

import CustomImage from "@components/CustomImage.astro";
interface Props extends HeroBlock {}
const { heading, excerpt, cover } = Astro.props;
---

<header class={s["bubble_images__header"]}>
    <Container>
        <div class={s["bubble_images__wrapper"]}>
            <article class={s["bubble_images__content"]}>
                <h1>{heading}</h1>
                <p>{excerpt}</p>
            </article>

            <div class={s["bubble_images__image_wrapper"]}>
                <CustomImage
                    image={cover}
                    size="large"
                    classlist={s["bubble_images__image"]}
                    eager={true}
                />
                <CustomImage
                    image={cover}
                    size="large"
                    classlist={s["bubble_images__image"]}
                    eager={true}
                />
                <CustomImage
                    image={cover}
                    size="large"
                    classlist={s["bubble_images__image"]}
                    eager={true}
                />
            </div>
        </div>
    </Container>
</header>